<Window x:Class="TransDemo.UI.Views.SearchCustomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:TransDemo.UI.Converters"
        Title="Search Customer" Height="400" Width="300" 
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <conv:NullToBoolConverter x:Key="NullToBoolConverter"/>
    </Window.Resources>
    <DockPanel Margin="10">
		<!-- Usunęliśmy PlaceholderText -->
		<TextBox DockPanel.Dock="Top"
                 Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,8"
                 ToolTip="Wpisz część nazwy, aby przefiltrować"/>
		<ListBox ItemsSource="{Binding Customers}"
                 SelectedItem="{Binding SelectedCustomer}"
                 DisplayMemberPath="FullName"/>
		<StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
			<Button Content="OK" Width="75" Margin="0,0,8,0"
                    IsEnabled="{Binding SelectedCustomer, Converter={StaticResource NullToBoolConverter}}"
                    Command="{Binding ConfirmCommand}"/>
			<Button Content="Cancel" Width="75"
                    Command="{Binding CancelCommand}"/>
		</StackPanel>
	</DockPanel>
</Window>
