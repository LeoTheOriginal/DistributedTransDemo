<UserControl x:Class="TransDemo.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:md="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             xmlns:conv="clr-namespace:TransDemo.UI.Converters"
             >

    <UserControl.Resources>
        <conv:ChartTypeToVisibilityConverter x:Key="ChartTypeToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Górny panel: wybór opcji -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Left" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Text="Typ wykresu:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding ChartTypes}"
                      SelectedItem="{Binding SelectedChartType}"
                      Width="120" Margin="0,0,16,0"/>

            <TextBlock Text="Ostatnie dni:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox ItemsSource="{Binding DayOptions}"
                      SelectedItem="{Binding SelectedDays}"
                      Width="80"/>
        </StackPanel>

        <!-- Przyciski akcji -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,0,0,10">
            <Button
                Command="{Binding RefreshCommand}"
                Content="Odśwież"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Margin="4"/>
        </StackPanel>

        <!-- Wykres -->
        <md:Card Grid.Row="2" Padding="16">
            <Grid>
                <lvc:CartesianChart x:Name="CartesianChart"
                        Series="{Binding ChartSeries}"
                        XAxes="{Binding XAxes}"
                        YAxes="{Binding YAxes}"
                        Visibility="{Binding SelectedChartType, Converter={StaticResource ChartTypeToVisibilityConverter}, ConverterParameter=Cartesian}" />
                

                <!-- Pie (kołowy udział oddziałów) -->
                <lvc:PieChart x:Name="PieChart"
                      Series="{Binding ChartSeries}"
                      Visibility="{Binding SelectedChartType, Converter={StaticResource ChartTypeToVisibilityConverter}, ConverterParameter=Pie}" />
            </Grid>
        </md:Card>
    </Grid>
</UserControl>
